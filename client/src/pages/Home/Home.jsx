import React from "react";
import styles from "../../Global";

const Home = () => {
  return (
    <div className={`${styles.superContainer}`}>
      <div className="h-[100px] bg-primary w-screen flex"></div>
    </div>
  );
};

export default Home;
